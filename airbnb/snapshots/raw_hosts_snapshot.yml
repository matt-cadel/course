snapshots:
  - name: scd_raw_hosts
    description: Snapshot of the raw hosts table to capture changes over time
    relation: source('airbnb', 'hosts')
    config:
      unique_key: id
      strategy: timestamp
      updated_at: updated_at
      hard_deletes: invalidate
