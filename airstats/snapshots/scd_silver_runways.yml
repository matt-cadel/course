snapshots:
  - name: scd_silver_runways
    description: Snapshot of the silver runways table to capture changes over time
    relation: ref('silver_runways')
    config:
      unique_key: runway_id
      strategy: check
      check_cols: all