snapshots:
  - name: scd_silver_airports
    description: Snapshot of the silver airports table to capture changes over time
    relation: ref('silver_airports')
    config:
      unique_key: airport_ident
      strategy: check
      check_cols: all